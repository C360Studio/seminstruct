name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:

jobs:
  build:
    name: Build and Test
    runs-on: ubuntu-latest
    timeout-minutes: 45  # Allow time for llama-server build + model download

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build seminstruct (llama-server + GGUF) from source
        run: |
          echo "Building seminstruct image with default Qwen3-0.6B Q4_K_M..."
          docker compose -f docker-compose.yml -f docker-compose.ci.yml build

      - name: Start service
        run: |
          docker compose -f docker-compose.yml -f docker-compose.ci.yml up -d

      - name: Wait for model load
        run: |
          echo "Waiting for llama-server to load the GGUF and start serving..."
          for i in {1..90}; do
            if curl -sf http://localhost:8083/health 2>/dev/null; then
              echo "seminstruct is healthy"
              exit 0
            fi
            echo "Attempt $i/90: model still loading..."
            sleep 5
          done
          echo "seminstruct failed to start"
          docker compose logs seminstruct
          exit 1

      - name: Verify model alias is published
        run: |
          models=$(curl -s http://localhost:8083/v1/models)
          echo "Models response: $models"
          echo "$models" | jq -e '.data[] | select(.id == "qwen3-0.6b")' > /dev/null \
            || (echo "Expected model id 'qwen3-0.6b' not found in /v1/models" && exit 1)

      - name: Test chat completions endpoint (integration)
        run: |
          response=$(curl -s -X POST http://localhost:8083/v1/chat/completions \
            -H "Content-Type: application/json" \
            -d '{"model":"qwen3-0.6b","messages":[{"role":"user","content":"Say hello"}],"max_tokens":20,"stream":false}')
          echo "Response: $response"
          echo "$response" | jq -e '.choices[0].message.content' > /dev/null \
            || (echo "No content in response" && exit 1)
          echo "Integration test passed."

      - name: View seminstruct logs
        if: always()
        run: docker compose -f docker-compose.yml -f docker-compose.ci.yml logs seminstruct

      - name: Stop service
        if: always()
        run: docker compose -f docker-compose.yml -f docker-compose.ci.yml down

  security:
    name: Security Scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: 'fs'
          scan-ref: '.'
          format: 'sarif'
          output: 'trivy-results.sarif'

      - name: Upload Trivy results to GitHub Security
        uses: github/codeql-action/upload-sarif@v4
        if: always()
        with:
          sarif_file: 'trivy-results.sarif'

  lint:
    name: Lint Dockerfile
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Lint Dockerfile
        uses: hadolint/hadolint-action@v3.1.0
        with:
          dockerfile: Dockerfile
          # DL3008,DL3009: apt-get version pinning warnings
          # DL3013: pip install version pinning — we deliberately float
          #   huggingface-hub since it's a single-purpose build-stage tool
          #   and HF API changes are easier to track than HF-pinned breakage.
          ignore: DL3008,DL3009,DL3013

  push:
    name: Push to GHCR (${{ matrix.variant.alias }})
    runs-on: ubuntu-latest
    needs: [build, security, lint]
    # Run on every main push and on manual dispatch. BuildKit GHA cache
    # keeps unchanged-input rebuilds fast (~1-2 min per variant from
    # cache), so always-run is a cleaner contract than fragile
    # change-detection on the head commit.
    if: |
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'push' && github.ref == 'refs/heads/main')
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        variant:
          # Hot tier: small + fast for intent / classification / defaults.model.
          # reasoning=off so Qwen3 doesn't burn tokens on chain-of-thought
          # prefix before the actual classification token. Aliased as :latest
          # since single-endpoint deployments most often want the hot path.
          # parallel=4, context=16384 → 4096 tokens per slot, plenty for
          # short classification prompts.
          - alias: qwen3-0.6b
            repo: unsloth/Qwen3-0.6B-GGUF
            file: Qwen3-0.6B-Q4_K_M.gguf
            reasoning: off
            parallel: 4
            context: 16384
            extra_tags: |
              type=raw,value=latest,enable={{is_default_branch}}
          # Memory-constrained summary fallback. Substitute for :qwen3-8b
          # when the host can't fit 8B; expect quality degradation in
          # graph-persisted summaries. reasoning=auto.
          - alias: qwen3-1.7b
            repo: unsloth/Qwen3-1.7B-GGUF
            file: Qwen3-1.7B-Q4_K_M.gguf
            reasoning: auto
            parallel: 4
            context: 16384
            extra_tags: ""
          # Recommended summary tier: 8B for community_summary /
          # answer_synthesis. parallel=2 (fewer concurrent slots since
          # each summary is heavier) with context=16384 → 8192 tokens per
          # slot, comfortable for graph-context summaries. ~9GB process
          # memory. reasoning=auto.
          - alias: qwen3-8b
            repo: unsloth/Qwen3-8B-GGUF
            file: Qwen3-8B-Q4_K_M.gguf
            reasoning: auto
            parallel: 2
            context: 16384
            extra_tags: ""
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/c360studio/seminstruct
          tags: |
            type=raw,value=${{ matrix.variant.alias }},enable={{is_default_branch}}
            type=raw,value=${{ matrix.variant.alias }}-{{sha}},enable={{is_default_branch}}
            ${{ matrix.variant.extra_tags }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            MODEL_REPO=${{ matrix.variant.repo }}
            MODEL_FILE=${{ matrix.variant.file }}
            MODEL_ALIAS=${{ matrix.variant.alias }}
            MODEL_REASONING=${{ matrix.variant.reasoning }}
            MODEL_PARALLEL=${{ matrix.variant.parallel }}
            MODEL_CONTEXT=${{ matrix.variant.context }}
          # Share only the llama.cpp builder stage cache across matrix
          # entries. We previously also cached per-variant layers, but
          # the combined cache footprint (~10GB across builder + 3
          # variants × multi-arch) hit GHA's 10GB-per-repo cap and
          # evictions mid-build caused BlobNotFound failures on the 8B
          # variant. Per-variant caching gave little real benefit anyway:
          # each variant has a unique model file, so cross-variant cache
          # hits aren't possible at the runtime-stage layers.
          cache-from: type=gha,scope=seminstruct-builder
          cache-to: type=gha,scope=seminstruct-builder,mode=max
